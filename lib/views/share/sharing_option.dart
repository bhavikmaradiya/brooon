class SharingOption {
  int settingId;
  String title;
  String subtitle;
  bool isEnabled;

  SharingOption({
    required this.settingId,
    required this.title,
    required this.subtitle,
    required this.isEnabled,
  });
}
